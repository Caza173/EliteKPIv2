{
  "name": "elitekpi-production",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build",
    "start": "cross-env NODE_ENV=production node dist/index.js",
    "dev": "tsx --env-file .env server/index.ts",
    "check": "tsc",
    "db:push": "drizzle-kit push"
  }
}
